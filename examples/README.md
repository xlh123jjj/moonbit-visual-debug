# Examples

## Detection Report

Run:

```bash
moon run --target wasm-gc examples/detection_report > detection-report.html
```

The example compares expected and predicted boxes, creates an error layer for a label mismatch, and prints a self-contained HTML report.
